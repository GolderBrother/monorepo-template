import { CoreResponse } from './types';

/**
 * 反转字符串
 * @param str 字符串
 * @returns 反转后的字符串
 * @example reverseString('hello') => 'olleh'
 */
export declare const reverseString: (str: unknown) => string;
export declare const coreFunction: () => CoreResponse;
export * from './types';
